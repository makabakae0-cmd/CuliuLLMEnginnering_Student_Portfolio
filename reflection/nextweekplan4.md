finish优化修改自动演示（ai vs ai）：
1.优化宿主 AI 行为：
现在宿主 AI 基本是朝巢穴走，比较像“贪心路线”。
下一步可以加入更自然的策略：优先接近巢穴、避免频繁换层、如果连续移动无效就换方向。
展示时让宿主看起来像“在思考和逃生”，不是机械移动。
优化真菌 AI 布阵

2. 保留 GLM-5 生成孢子策略：
增加布阵校验机制：必须生成 10 个孢子，孢子不能过度集中，不能全部堆叠在同一层，并且应尽量分布在宿主通往巢穴的可能路径附近。
同时加入公平性限制：真菌方不能预判或提前知道宿主的出生点，只能基于游戏规则和当前可见信息生成布阵策略，避免 AI 布阵变成“开图式预测”。
如果模型输出的 JSON 不规范，系统应自动修复或 fallback 到备用布阵逻辑，保证 demo 不会因为格式错误而卡住。
增强演示可视化：让孢子布阵、校验结果、fallback 过程和 AI 策略意图更清楚地展示给观众。

